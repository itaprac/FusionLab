function ExampleMLFusion({ darkMode }) {
  return (
    <div>
      <div className="mb-6">
        <h2 className={`text-lg font-semibold ${darkMode ? 'text-white' : 'text-gray-900'}`}>
          Examples → MLFusion
        </h2>

      </div>

      <div className={`rounded-2xl border p-6 ${darkMode ? 'bg-gray-900 border-gray-800' : 'bg-white border-gray-200'}`}>
        <div className={`text-sm font-semibold ${darkMode ? 'text-gray-100' : 'text-gray-900'}`}>
          Soon...
        </div>

      </div>
    </div>
  )
}

export default ExampleMLFusion
