function Docs({ darkMode }) {
  return (
    <div className={`py-12 text-center ${darkMode ? 'text-gray-400' : 'text-gray-500'}`}>
      <p>Documentation will be added here.</p>
    </div>
  )
}

export default Docs
